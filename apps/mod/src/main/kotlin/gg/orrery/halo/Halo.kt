package gg.orrery.halo

/**
 * Halo — HUD layer (stub, Phase 0).
 *
 * Phase 3 will implement: cosmetic/status overlays built on Lumen — stats already
 * visible to the player, item/price info from Sextant, custom scoreboard/action-bar
 * replacements (DESIGN_SPEC §6.6).
 *
 * Hard rule from §2: no minimaps, no other-player indicators, no hidden-info surfacing.
 * Halo renders ONLY data the server already sent to this client or data from Sextant's
 * Hypixel public API proxy.
 */
object Halo
