package gg.orrery.lumen

/**
 * Lumen — Custom UI toolkit (stub, Phase 0).
 *
 * Phase 2 will implement: retained-mode component tree, flexbox-ish layout,
 * theming from Tokens.kt (DESIGN_SPEC §5 — zero hardcoded colors, zero vanilla chrome),
 * MSDF custom font rendering, and animation (§6.4).
 *
 * Lumen has NO dependency on network/Relay/Eclipse; it only fires input callbacks
 * that Eclipse authorizes into the clickSlot chokepoint (§11).
 */
object Lumen
