package gg.orrery.codex

/**
 * Codex — Config system (stub, Phase 0).
 *
 * Phase 3 will implement: on-disk JSON/TOML config, schema-versioned with migrations,
 * per-profile and per-account scoping. The launcher (Observatory) writes config before
 * launch; Relay can push live updates while the game runs (DESIGN_SPEC §6.7).
 */
object Codex
