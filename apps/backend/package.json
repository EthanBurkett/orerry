{
  "name": "@orrery/backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cargo build",
    "dev": "cargo run -p horizon",
    "test": "cargo test",
    "lint": "cargo clippy",
    "check": "cargo check"
  }
}
